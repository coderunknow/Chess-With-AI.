/**
 * Product metadata shared by the runtime, the manifest and the docs.
 *
 * @module shared/meta
 */

/** Marketing/brand name shown in the UI and in log prefixes. */
export const APP_NAME = "AI Chess Companion";

/** Machine name used for storage keys and package metadata. */
export const APP_ID = "ai-chess-companion";

/** Current extension version. Kept in sync with `manifest.json` by CI. */
export const APP_VERSION = "0.5.0";

/** Repository URL, referenced from the docs and the manifest. */
export const REPOSITORY_URL = "https://github.com/coderunknow/Chess-With-AI";
